/// Dimension constants for fixed widget sizes (heights, widths, radii).
///
/// Use these for component sizes instead of hard-coded numbers.
library;

class Dimensions {
  // Logo & avatar sizes
  static const double logoSize = 120.0;
  static const double profileAvatarRadius = 18.0;

  // Dashboard specific sizes
  static const double vehicleIconContainerSize = 56.0;
  static const double vehicleIconSize = 32.0;
  static const double healthIndicatorSize = 72.0;

  // Navigation
  static const double bottomNavHeight = 64.0;

  // Private constructor
  Dimensions._();
}

